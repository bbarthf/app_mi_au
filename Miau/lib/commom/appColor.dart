import 'dart:ui';
import 'package:flutter/material.dart';

class AppColors {
  const AppColors();

  // texto
  static const Color text = Colors.white;

  // botão
  static const Color bottons = Colors.green;

  // card principal
  static const Color cardSlide = const Color(0xFFFFFFFF);

  // fundo do programa
  static const Color backgroud = const Color(0xFFFFA726);

  static const Color secundary = const Color(0xFF597EFF);
  static const Color backgndAvatar = Colors.amber;
}
